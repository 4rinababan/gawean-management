@using Microsoft.AspNetCore.Identity
@using TaskManagement.Infrastructure.Identity

@inject SignInManager<ApplicationUser> SignInManager

<nav class="flex flex-col gap-1 text-sm">
    <NavLink class="tm-manage-link" href="Account/Manage" Match="NavLinkMatch.All">Profile</NavLink>
    <NavLink class="tm-manage-link" href="Account/Manage/Email">Email</NavLink>
    <NavLink class="tm-manage-link" href="Account/Manage/ChangePassword">Password</NavLink>
    @if (hasExternalLogins)
    {
        <NavLink class="tm-manage-link" href="Account/Manage/ExternalLogins">External logins</NavLink>
    }
    <NavLink class="tm-manage-link" href="Account/Manage/TwoFactorAuthentication">Two-factor authentication</NavLink>
    <NavLink class="tm-manage-link" href="Account/Manage/Passkeys">Passkeys</NavLink>
    <NavLink class="tm-manage-link" href="Account/Manage/PersonalData">Personal data</NavLink>
</nav>

@code {
    private bool hasExternalLogins;

    protected override async Task OnInitializedAsync()
    {
        hasExternalLogins = (await SignInManager.GetExternalAuthenticationSchemesAsync()).Any();
    }
}
